<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ app_name }}{% endblock %}</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.svg">
    <meta name="theme-color" content="#2d8f3e">
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
    <style>
        /* Mediahuus Form Styling */
        .mediahuus-input {
            @apply block rounded-lg shadow-sm text-lg;
            @apply transition-all duration-200 ease-in-out;
            @apply focus:ring-2 focus:ring-green-500 focus:border-green-500 focus:outline-none;
            @apply hover:border-green-400;
            border: 2px solid #2d8f3e !important;
            background-color: white !important;
            width: 100% !important;
            padding: 1rem 1.5rem !important;
            min-height: 56px;
        }
        .mediahuus-input.error, .mediahuus-select.error, .mediahuus-textarea.error {
            border: 2px solid #f87171 !important;
            @apply focus:ring-red-500 focus:border-red-500;
        }
        .mediahuus-select {
            @apply block rounded-lg shadow-sm text-lg;
            @apply transition-all duration-200 ease-in-out;
            @apply focus:ring-2 focus:ring-green-500 focus:border-green-500 focus:outline-none;
            @apply hover:border-green-400;
            border: 2px solid #2d8f3e !important;
            background-color: white !important;
            width: 100% !important;
            padding: 1rem 1.5rem !important;
            min-height: 56px;
        }
        .mediahuus-textarea {
            @apply block rounded-lg shadow-sm text-lg;
            @apply transition-all duration-200 ease-in-out resize-vertical;
            @apply focus:ring-2 focus:ring-green-500 focus:border-green-500 focus:outline-none;
            @apply hover:border-green-400;
            border: 2px solid #2d8f3e !important;
            background-color: white !important;
            width: 100% !important;
            padding: 1rem 1.5rem !important;
            min-height: 240px !important;
            height: 240px;
        }
        .mediahuus-label {
            @apply block text-base font-semibold text-gray-700 mb-3;
            display: block !important;
            width: 100%;
        }
        .mediahuus-button-primary {
            background-color: #2d8f3e !important;
            color: white !important;
            padding: 0.75rem 1.5rem !important;
            border-radius: 0.5rem !important;
            font-size: 1rem !important;
            font-weight: 600 !important;
            box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05) !important;
            transition: all 0.2s ease-in-out !important;
            border: 2px solid #2d8f3e !important;
            cursor: pointer !important;
            display: inline-block !important;
            text-decoration: none !important;
            text-align: center !important;
        }
        .mediahuus-button-primary:hover {
            background-color: #1f5f2a !important;
            border-color: #1f5f2a !important;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1) !important;
        }
        .mediahuus-button-secondary {
            background-color: white !important;
            color: #2d8f3e !important;
            padding: 0.75rem 1.5rem !important;
            border-radius: 0.5rem !important;
            font-size: 1rem !important;
            font-weight: 600 !important;
            box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05) !important;
            transition: all 0.2s ease-in-out !important;
            border: 2px solid #2d8f3e !important;
            cursor: pointer !important;
            display: inline-block !important;
            text-decoration: none !important;
            text-align: center !important;
        }
        .mediahuus-button-secondary:hover {
            background-color: #f0f9f0 !important;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1) !important;
        }
        .mediahuus-form-section {
            @apply space-y-6 p-8 bg-white rounded-xl shadow-sm border border-gray-100;
        }
        .mediahuus-richtext {
            border: 2px solid #2d8f3e;
            border-radius: 0.5rem;
            min-height: 240px;
            background: white;
        }
        .mediahuus-richtext .ql-toolbar {
            border-radius: 0.5rem 0.5rem 0 0;
            border: 2px solid #2d8f3e;
            border-bottom: none;
            background: #f0fdf4;
        }
        .mediahuus-richtext .ql-container {
            border-radius: 0 0 0.5rem 0.5rem;
            border: 2px solid #2d8f3e;
            border-top: none;
            min-height: 200px;
            background: white;
        }
        .mediahuus-richtext .ql-editor {
            min-height: 200px;
            font-size: 16px;
            line-height: 1.6;
            padding: 1.25rem 1rem;
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    {% block header %}
    {% if user %}
    <nav class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/" class="flex items-center hover:opacity-75 transition-opacity">
                        <img src="/assets/logo.png" alt="{{ app_name }}" class="h-12 w-auto">
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-sm text-gray-700">{{ user.name }}</span>
                    <form method="POST" action="/logout" class="inline">
                        <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded-md text-sm hover:bg-red-700">
                            Abmelden
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </nav>
    {% endif %}
    {% endblock %}

    <main>
        {% block content %}{% endblock %}
    </main>

    {% block scripts %}{% endblock %}
</body>
</html>